package Commands;

public class Konec implements Command {
    @Override
    public String execute(String[] args) {
        // TODO provedeni prikazu konec
        return null;
    }

    @Override
    public boolean exit() {
        return true;
    }
}
